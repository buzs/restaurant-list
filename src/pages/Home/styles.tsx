import styled from "styled-components";

export const Wrapper = styled.div`
    margin: 0 auto;
    max-width: 70rem;
    background: rgba(255, 255, 255, 0.32);
    padding: 3rem;
    border-radius: 1rem;
`